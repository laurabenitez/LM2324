function validar(elementos){
    let estanCorrectos = true;
    for (var i=0; i<elementos.lenght;i++){
        if (elementos{i}.value ==""){
            document.getElementById("campo"+toString(i+1)).
        }
    }

}